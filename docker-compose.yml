version: '3'
services:
  bot:
    image: python:3.8
    command: python tiktokinfo.py
    tty: true
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHANNEL=${CHANNEL}
      - ADMIN=${ADMIN}
      - CHANNEKS=${CHANNEKS}
      
